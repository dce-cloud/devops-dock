# 0.3.x 系列使用的是6字段格式，秒分时 日月周, 必须有秒
[job-local "ofelia-date"]
schedule = @every 1m
command = date -R
dir = /tmp/

# ----------------- example -----------------
[job-exec "example"]
schedule = @every 24h
container = example
command = php example.php
user = www-data
tty = false

